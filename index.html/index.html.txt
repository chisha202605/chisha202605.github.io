<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>家庭共享菜谱</title>
  <link rel="stylesheet" href="style.css"/>
</head>
<body>
  <div class="container">
    <header class="header">
      <h1>家庭共享菜谱</h1>
      <div class="nav">
        <button onclick="showPage('home')">首页</button>
        <button onclick="showPage('addDish')">新增菜品</button>
        <button onclick="showPage('meal')">配餐</button>
        <button onclick="showPage('mine')">我的</button>
      </div>
    </header>

    <!-- 首页 -->
    <div id="page-home" class="page">
      <div class="search"><input id="searchInput" placeholder="搜索菜名/食材" oninput="renderDish()"/></div>
      <div class="category-nav" id="categoryNav"></div>
      <div class="dish-list" id="dishList"></div>
    </div>

    <!-- 新增/编辑菜品 -->
    <div id="page-addDish" class="page hidden">
      <h2>新增菜品</h2>
      <div class="form">
        <input id="dishName" placeholder="菜名"/>
        <select id="dishCategory"></select>
        <select id="dishDiff">
          <option>简单</option><option>中等</option><option>困难</option>
        </select>
        <input id="dishTime" placeholder="烹饪耗时"/>
        <input id="dishPeople" placeholder="适合人数"/>
        <textarea id="dishIng" placeholder="食材：名称+用量，换行分隔"></textarea>
        <textarea id="dishStep" placeholder="详细做法"></textarea>
        <input id="dishVideo" placeholder="视频链接，多个换行"/>
        <button onclick="saveDish()">保存菜品</button>
      </div>
    </div>

    <!-- 配餐页 -->
    <div id="page-meal" class="page hidden">
      <h2>配餐中心</h2>
      <button onclick="randomMeal()">随机4菜1汤（3天不重复，含荤菜）</button>
      <button onclick="manualMeal()">手动选菜配餐</button>
      <div id="mealResult" class="meal-result"></div>
      <div id="ingredientCheck" class="ingredient-check"></div>
    </div>

    <!-- 个人中心 -->
    <div id="page-mine" class="page hidden">
      <h2>我的</h2>
      <button onclick="showRecycle()">菜品回收站</button>
      <button onclick="exportData()">导出备份</button>
      <button onclick="importData()">导入备份</button>
      <div id="recycleList"></div>
    </div>
  </div>
  <script src="app.js"></script>
</body>
</html>